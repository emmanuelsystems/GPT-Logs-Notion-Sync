# GPT → Notion Integration

Sync ChatGPT conversation logs (stored in `conversations/logs.md`) into a Notion page.

## Setup

### 0. Clone or open your repository
In your Codex or local environment, make sure you’re in the root of your **GPT-Logs-Notion-Sync** repo. If you haven’t yet:
```bash
git clone git@github.com:emmanuelsystems/GPT-Logs-Notion-Sync.git
cd GPT-Logs-Notion-Sync
